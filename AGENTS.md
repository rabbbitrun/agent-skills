# 仓库维护规则

- 所有可复用 skill 必须位于 `skills/<分类>/<skill-name>/`。沿用 README 中的分类，以主要用途选择一个分类。
- 修改已有 skill 时直接更新原目录，不创建 `v2`、日期副本或把文件散放到根目录。
- `SKILL.md` 的 `name` 必须与目录名一致；模板、脚本、参考资料和正式示例跟随所属 skill。
- 新增、删除或移动 skill 时同步更新 README 的 Skills 目录。新增分类时同步更新 README 和 `scripts/check_layout.py`。
- 预览草稿、临时截图、备份、依赖目录、缓存和凭证不提交。不要使用 `git add .` 批量收集未检查的文件；按明确路径暂存并审查差异。
- 提交前运行 `python3 scripts/check_layout.py`，并运行被修改 skill 适用的验证。目录检查基于 Git 已跟踪／已暂存的路径，新增文件应先明确暂存。
- 推送前检查当前仓库、分支和远端，遵循用户授权；未经请求不要改变仓库可见性、强制推送或删除远端内容。
